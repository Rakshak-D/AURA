// Search functionality (already implemented in tasks.js)
// This file can be extended for global search across all data

function globalSearch(query) {
    // Search across tasks, documents, chat history
    // Return unified results
}
